import { FaShoppingCart } from 'react-icons/fa';

const CartButton = () => {
  return (
    <button className="cart-button">
      <FaShoppingCart />
    </button>
  );
};

export default CartButton;
