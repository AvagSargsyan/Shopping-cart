import { StyledLoader } from './styles/Loader.style';

const Loader = () => {
  return (
    <StyledLoader>
      <div className="loader"></div>
      <p>Loading...</p>
    </StyledLoader>
  );
};

export default Loader;
